[project]
name = "base_project"
version = "0.1.0"
description = "Base project for stats software"
requires-python = ">=3.12"
dependencies = [
    "pika>=1.4.2",
]
    
[tool.uv.workspace]

members = [
    "common",
    "api-service",
    "python-service",
]
